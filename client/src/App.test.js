import { render, screen } from '@testing-library/react';
import App from './App';


test('Please add tests later', () => {
 /* render(<App />);
  const linkElement = screen.getByText("SEARCH FOR PHONE");
  expect(linkElement).toBeInTheDocument(); */
  expect(true).toBe(true)
});
